import math
def prime(n):
    if n==1:
        return False
    m=int(math.sqrt(n))
    for i in range(2,m+1):
        if n%i==0:
            return False
        return True
    a,b=map(int,input().split())
    c=0
    for i in range(a,b+1):
        if prime(i)==True:
            c=c+1
        print(c)        


output:
6 15
3
>
